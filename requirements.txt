streamlit>=1.36
pandas>=2.0
openpyxl>=3.1
rapidfuzz>=3.0
ddgs>=6.0
pypdf>=4.0
openai>=1.40
requests>=2.31
plotly>=6.0
python-dotenv>=1.0
fastapi>=0.111
uvicorn[standard]>=0.30
gunicorn
boto3
python-multipart
msal>=1.31
redis>=5.0
# TestClient needs httpx. It is present transitively today, so a new API test passes
# locally and fails in a clean image — hence the explicit pin.
httpx>=0.27
